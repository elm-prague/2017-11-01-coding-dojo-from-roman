{
  "name": "elm-prague-dojo2",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MPL-2.0",
  "scripts": {
    "dev": "run-s dev:start",
    "dev:start": "cross-env BROWSER=none elm-app start",
    "build": "run-s build:app",
    "build:app": "elm-app build",
    "elm:install": "elm-app install",
    "test": "elm-app test",
    "test:watch": "run-s \"test --watch\""
  },
  "devDependencies": {
    "create-elm-app": "^1.9.1",
    "npm-run-all": "^4.1.1"
  }
}
